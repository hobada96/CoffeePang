import React from "react";import {useRouter} from "next/router";const CoffeeConfig = () => {    const router = useRouter();    const {coffee} = router.query;    const {coffeeName, seller, price, weight, description} = JSON.parse(coffee);    return (        <>            <h1>커피 이름 : {coffeeName}</h1>            <h1>판매자 : {seller}</h1>            <h2>가격 : {price}</h2> <h2>무게 :{weight}</h2>            <h3>상세설명 : {description}</h3>        </>    )}export default CoffeeConfig;