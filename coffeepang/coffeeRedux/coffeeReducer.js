import {ADD_GOODS} from './coffeeAction'import firebase from "firebase/app";import fire from "../config/fire-config";const initalState = []export const coffeeReducer =(state=initalState, action)=>{    const {type,payload} = action;    switch (type){        case ADD_GOODS : {            fire.firestore()                .collection('COFFEE')                .doc()                .set(payload);            return (                state.concat(payload)            );        }        default : return state;    }}